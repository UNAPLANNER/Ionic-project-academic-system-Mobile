import { NgModule } from '@angular/core';
import { StudentTabsRoutingModule } from './student-tabs-routing.module';

@NgModule({
  imports: [StudentTabsRoutingModule]
})
export class StudentTabsModule {}
