import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.ionic.academicsystemuna',
  appName: 'sistema-mobile',
  webDir: 'www'
};

export default config;
